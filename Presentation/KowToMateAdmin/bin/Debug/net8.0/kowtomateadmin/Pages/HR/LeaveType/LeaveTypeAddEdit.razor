@using CutOutWiz.Services.Models.HR
@using static CutOutWiz.Core.Utilities.Enums

<div class="mb-2 row">
    <label for="Name" class="col-sm-4 col-form-label fw-bold">Name</label>
    <div class="col-sm-8">
        <InputText class="form-control" @bind-Value="leaveType.Name"></InputText>
        <ValidationMessage For="@(() => leaveType.Name)" />
    </div>
</div>

@* <div class="mb-2 row">
    <label for="IsActive" class="col-sm-4 col-form-label fw-bold">Is Active</label>
    <div class="col-sm-8">
      <InputSelect @bind-Value="leaveType.IsActive" class="form-select">
           <option value="0">- Select -</option>
        @foreach (var status in Enum.GetValues(typeof(GeneralStatus)))
        {
            <option value="@((int)status)">@status</option>
        }
         </InputSelect>
        <ValidationMessage For="@(() => leaveType.IsActive)" />
    </div>
</div> *@

<div class="mb-2 row">
    <label class="col-sm-4 col-form-label fw-bold">Is Active</label>
    <div class="col-sm-8">
        <label class="form-check-label mt-2" for="IsActive">
            <InputCheckbox @bind-Value="leaveType.IsActive" id="IsActive" class="form-check-input"></InputCheckbox>
        </label>
        <ValidationMessage For="@(() => leaveType.IsActive)" />
    </div>
</div>

@code {
    [Parameter]
    public LeaveTypeModel leaveType { get; set; }
}
